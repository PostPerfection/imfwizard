[project]
name = "imfwizard"
version = "1.2.0"
description = "Python bindings for IMF Wizard"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
